import { injectable } from "inversify";

@injectable()
export class RedisContext {
    constructor() {
        // redis = new Redis()
        // publisher = redis.createClient()

        // and some works 
    }
}